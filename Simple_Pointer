#include <stdio.h>

int main() {
    // 정수형 변수 선언 및 초기화
    int number = 10;
    
    // 포인터 선언 및 변수의 주소 할당
    int *ptr = &number;

    // 변수 값 출력
    printf("변수의 값: %d\n", number);

    // 포인터를 통해 변수 값 변경
    *ptr = 20;

    // 변경된 변수 값 출력
    printf("변경된 변수의 값: %d\n", number);

    return 0;
}

정수형 변수를 선언하고 해당 변수의 주소를 가리키는 포인터를 이용하여 값을 변경하는 프로그램이다.
